# Simulation mode by default; set to false ONLY when you intend to run live.
DRY_RUN=true

# Required only when DRY_RUN=false; must be 0x + 64 hex characters
PRIVATE_KEY=

# Network selection (adjust if multi-chain support is added)
NETWORK=mainnet

# Optional Erigon RPC (HTTP) and fallback RPC endpoints
ERIGON_RPC_HTTP=
FALLBACK_RPC_HTTP=