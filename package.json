{
  "name": "jit-liquidity-framework",
  "version": "0.0.1",
  "private": true,
  "description": "Seed scaffold for a Uniswap v3 Just-In-Time (JIT) liquidity simulation & strategy framework. (Core implementation to arrive in follow-up PR.)",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "dev": "echo \"src/index.ts will be added in next PR\" && exit 0",
    "lint": "eslint . --ext .ts,.js",
    "prepare": "echo \"(husky hook placeholder – none yet)\""
  },
  "dependencies": {
    "pino": "^8.21.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2",
    "vitest": "^1.5.3"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
