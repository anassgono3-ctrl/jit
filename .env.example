# Environment Configuration Example
# Copy this file to .env and update values for production

# ===== OPERATIONAL CONFIGURATION =====

# Runtime Mode (IMPORTANT: Keep DRY_RUN=true for testing)
DRY_RUN=true
NETWORK=mainnet

# ===== MULTI-RPC FAILOVER CONFIGURATION =====

# Option 1: Multi-RPC failover with comma-separated URLs (recommended)
RPC_HTTP_LIST=https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY,https://rpc.ankr.com/eth,https://cloudflare-eth.com

# Option 2: Multi-RPC with custom weights (JSON format)
# RPC_HTTP_LIST=[{"url":"https://premium-rpc.com","weight":3},{"url":"https://backup-rpc.com","weight":1}]

# Legacy single provider approach (still supported)
PRIMARY_RPC_HTTP=https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY
FALLBACK_RPC_HTTP=https://rpc.ankr.com/eth
WS_RPC_URL=wss://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# Enhanced mempool monitoring (optional but recommended)
ERIGON_RPC_HTTP=http://localhost:8545

# ===== EIP-1559 GAS STRATEGY =====

# Base fee multiplier (e.g., 2.0 = 2x latest baseFee)
GAS_BASEFEE_BUMP=2.0

# Priority fee bounds (gwei)
PRIORITY_FEE_GWEI_MIN=1         # Minimum priority fee
PRIORITY_FEE_GWEI_MAX=3         # Maximum priority fee (prevents overpaying)

# ===== FLASHBOTS INTEGRATION =====

# Flashbots simulation endpoint
FLASHBOTS_RPC_URL=https://relay.flashbots.net

# Simulation timeout (milliseconds)
SIM_TIMEOUT_MS=3000

# ===== METRICS & OBSERVABILITY =====

# Prometheus metrics endpoint port
METRICS_PORT=9090

# Health check endpoint port  
HEALTH_PORT=9091

# Logging configuration
LOG_LEVEL=info                  # debug, info, warn, error
LOG_PRETTY=true                 # Pretty print logs (disable in production)

# ===== PROFITABILITY CONTROLS =====

# Profit thresholds (both must be met)
MIN_PROFIT_USD=25               # Minimum profit in USD
MIN_PROFIT_ETH=0.01             # Minimum profit in ETH

# Gas controls (legacy - use GAS_BASEFEE_BUMP instead)
MAX_PRIORITY_FEE_GWEI=50

# ===== SECURITY =====

# NEVER commit real private keys! Use a separate secure config for production
# Only required when DRY_RUN=false
PRIVATE_KEY=

# ===== LEGACY COMPATIBILITY =====

# Existing bot settings (maintained for backward compatibility)
ETH_RPC_URL=https://mainnet.infura.io/v3/YOUR_PROJECT_ID
ETH_RPC_WS_URL=wss://mainnet.infura.io/ws/v3/YOUR_PROJECT_ID
FLASHLOAN_PRIVATE_KEY=
DB_PATH=./data/jit_bot.db
LOG_FILE=./logs/jit_bot.log
MIN_SWAP_USD_003=15000
MIN_SWAP_USD_0005=70000
MIN_NET_PROFIT_USD=25
GAS_ESTIMATE_USD=15
NODE_ENV=development
ENABLE_BACKTEST=true