# Environment Configuration Example
# Copy this file to .env and update values for production

# ===== OPERATIONAL CONFIGURATION =====

# Runtime Mode (IMPORTANT: Keep DRY_RUN=true for testing)
DRY_RUN=true
NETWORK=mainnet

# ===== RPC CONFIGURATION =====

# Primary approach: Single providers with fallback
PRIMARY_RPC_HTTP=https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY
FALLBACK_RPC_HTTP=https://rpc.ankr.com/eth
WS_RPC_URL=wss://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# ===== FORK TESTING =====
# Real Balancer Vault mainnet-fork tests
# Required:
# FORK_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
#
# Optional stability knobs (the test will also try latest/other blocks automatically):
# Comma-separated list of blocks to try before falling back to latest (the test will stop at FORK_MAX_RESETS attempts)
# FORK_RESET_BLOCKS=19350000,19340000
# Maximum resets/attempts across candidate blocks (default 4)
# FORK_MAX_RESETS=4
#
# Probe time budget per block (ms) before bailing out to next block or skipping (default 25000)
# FORK_PROBE_BUDGET_MS=25000
#
# Floors per token (defaults are tiny and safe):
# WETH/DAI/BAL expressed in whole tokens; USDC/USDT expressed in base units (6 decimals)
# FORK_TEST_MIN_WETH=0.001
# FORK_TEST_MIN_DAI=0.1
# FORK_TEST_MIN_BAL=0.1
# FORK_TEST_MIN_USDC=100000    # 0.1 USDC
# FORK_TEST_MIN_USDT=100000    # 0.1 USDT
#
# Verbose probe logs (true/false)
# FORK_TEST_VERBOSE=false
#
# Strict mode: fail instead of skip when unsafe
# FORK_STRICT=false
#
# Increase Mocha timeout for longer-running fork tests (ms):
# MOCHA_TIMEOUT_MS=120000

# Alternative: JSON array of weighted providers
# RPC_PROVIDERS=[{"url":"https://rpc1.com","weight":2},{"url":"https://rpc2.com","weight":1}]

# Enhanced mempool monitoring (optional but recommended)
ERIGON_RPC_HTTP=http://localhost:8545

# ===== SECURITY =====

# NEVER commit real private keys! Use a separate secure config for production
# Only required when DRY_RUN=false
PRIVATE_KEY=

# ===== PROFITABILITY CONTROLS =====

# Profit thresholds (both must be met)
MIN_PROFIT_USD=25
MIN_PROFIT_ETH=0.01

# Gas controls
MAX_PRIORITY_FEE_GWEI=50

# ===== OBSERVABILITY =====

# Logging configuration
LOG_LEVEL=info
LOG_PRETTY=true

# Health monitoring
HEALTH_PORT=9090

# ===== LEGACY COMPATIBILITY =====

# Existing bot settings (maintained for backward compatibility)
ETH_RPC_URL=https://mainnet.infura.io/v3/YOUR_PROJECT_ID
ETH_RPC_WS_URL=wss://mainnet.infura.io/ws/v3/YOUR_PROJECT_ID
FLASHLOAN_PRIVATE_KEY=
DB_PATH=./data/jit_bot.db
LOG_FILE=./logs/jit_bot.log
METRICS_PORT=9090
MIN_SWAP_USD_003=15000
MIN_SWAP_USD_0005=70000
MIN_NET_PROFIT_USD=25
GAS_ESTIMATE_USD=15
NODE_ENV=development
ENABLE_BACKTEST=true